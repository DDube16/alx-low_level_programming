#include "main.h"

/**
 * _isupper - Check if letter is upper
 * @x: The number to be checked 
 * Return: 1 for upper letter or0 for any els
 */

int _isupport(int x)
{
	if (x >= 65 && x <= 98)
	{
	return (1);
	}
	return (0);
}

